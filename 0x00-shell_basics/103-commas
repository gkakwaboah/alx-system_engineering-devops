#!/bin/bash
ls -1ap | grep -v / | sort -d | tr '\n' ',' | sed 's/,$/\n/' && ls -1ap | grep / | grep -vE '^(\./|\.\./)' | sort -d | tr '\n' ',' | sed 's/,$/\n/' && ls -1ap | grep / | grep -E '^(\./|\.\./)' #The command lists all the files and directories in the current directory, separated by commas. It also includes hidden files and directories that start with a dot (.). The output is sorted alphabetically, except for directories . and … which are listed at the beginning. Digits come before letters.
