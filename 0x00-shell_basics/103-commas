#!/bin/bash
ls -Ap | grep -v /$ | sort -k1,1d -k1,1 | sed 's/^/"/g' | sed 's/$/",/g' | tr -d '\n' | sed 's/,$/\n/g'  #The command lists all the files and directories in the current directory, separated by commas. It also includes hidden files and directories that start with a dot (.). The output is sorted alphabetically, except for directories . and … which are listed at the beginning. Digits come before letters.
